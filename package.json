{
  "name": "horizon-backstage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "media:build": "bash scripts/optimize-media.sh",
    "share-pages:build": "node scripts/write-share-pages.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify-site.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
